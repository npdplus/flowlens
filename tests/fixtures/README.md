# Test Fixtures

This directory is reserved for synthetic, deterministic FlowLens test fixtures.

P01 does not add workflow-domain fixtures. Real fixtures are introduced by the phases that own the
relevant workflow contracts and behavior.
